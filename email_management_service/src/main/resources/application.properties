# ===============================
# Server Configuration
# ===============================
server.port=8086

# ===============================
# Application Name (Eureka)
# ===============================
spring.application.name=EMAIL-SERVICE

# ===============================
# Database Configuration (MySQL)
# ===============================
spring.datasource.url=jdbc:mysql://localhost:3306/emailDBM
spring.datasource.username=evilan
spring.datasource.password=evilan
spring.jpa.hibernate.ddl-auto=update

# ===============================
# Eureka Client Configuration
# ===============================
eureka.client.service-url.defaultZone=http://localhost:8761/eureka/
eureka.client.register-with-eureka=true
eureka.client.fetch-registry=true

# IMPORTANT: let Eureka register using hostname, not IP
eureka.instance.prefer-ip-address=false
# optional but explicit:
# eureka.instance.hostname=localhost

# ===============================
# Mail Configuration (Gmail SMTP)
# ===============================
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=elangaming994@gmail.com
spring.mail.password=ewerjqossfafssgs
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000

# ===============================
# Actuator (Optional but Recommended)
# ===============================
management.endpoints.web.exposure.include=health,info

